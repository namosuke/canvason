{
  "name": "canvason",
  "version": "0.0.2",
  "description": "A canvas library for generating image from JSON query",
  "author": "https://github.com/namosuke",
  "homepage": "https://github.com/namosuke/canvason",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^18.7.16",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  },
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js || echo NOTE: run `yarn build` first",
    "prepare": "yarn build"
  }
}
